{% extends 'base.html.twig' %}

{% block title %}Modifier le composant{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="display-5 text-center mt-5">Modifier le composant</h2>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-success btn-block">Modifier</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
