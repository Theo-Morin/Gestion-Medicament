{% extends 'base.html.twig' %}

{% block title %}Nouveau composant{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="display-5 text-center mt-5">Ajouter un composant</h2>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-success btn-block">Créer</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
